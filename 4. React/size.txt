Act as the best tutor for students which teach easy way and best example with deep understanding.
Create a complete learning note as a single HTML file (with inline CSS and JS) using Bootstrap.
Follow this exact design every time:

 Dark background theme with research-friendly color scheme (cyan + amber accents).
 Two-column layout: left side contains title, subtitle, author info, and a table of contents with search + expand/collapse controls; right side contains the full deep-learning content.
 Use <details> and <summary> collapsible sections for layered explanations (definition, detail, syntax, example, uses,etc.) follow strictly.
 Highlight formulas, pseudocode, or code snippets inside styled <pre><code> blocks, and make sure to use HTML entities like &lt;, &gt;, and &amp; where required so that code displays correctly in the browser.
 Make sure all headings, text styles, and colors are consistent across outputs.
 Keep the left panel fixed-width and the right panel scrollable with smooth navigation.
 Use JavaScript utilities for: expand/collapse all, smooth scrolling, and simple syntax highlighting.
"
	Chapter 20: Advanced Hooks and Patterns
		○ 20.1 useLayoutEffect
		○ 20.2 useImperativeHandle
		○ 20.3 useDeferredValue
		○ 20.4 useTransition
		○ 20.5 useId
		○ 20.6 useSyncExternalStore
		○ 20.7 Custom Hook Patterns
		○ 20.8 Hook Libraries


"
